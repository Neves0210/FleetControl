[phases.setup]
nixPkgs = ["nodejs_23"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["node ./node_modules/vite/bin/vite.js build"]

[start]
cmd = "node ./node_modules/vite/bin/vite.js preview --host 0.0.0.0 --port ${PORT:-8080}"