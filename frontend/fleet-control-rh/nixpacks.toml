    [phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["chmod +x ./node_modules/.bin/vite", "npm run build"]

[start]
cmd = "npx vite preview --host 0.0.0.0 --port $PORT"